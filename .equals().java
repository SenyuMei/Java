// para hacer comparaciones en vez de usar == se puede usar 
(nombre_variable1.equals(nombre_variable2);
// es lo mismo que decir
nombre_variable1 == nombre_variable2;
System.out.println(nombre_variable1.equals(nombre_variable2));
// el output sera false o true dependiendo de si la sentencia de que vairable 1 y variable sean == o !=
